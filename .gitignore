# Windows thumbnail cache files
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db

# Vs code directory automatic generated file
.dist

# The library folder which is very large to commit
lib/vendor/php-ffmpeg/

# The output folder where convertion rotation upload every video is stored
uploads

# These are the files or folders which doesn't need to be tracked.